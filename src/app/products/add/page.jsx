import ProductForm from "@/components/products/addProduct-form";
import React from "react";

const page = () => {
  return (
    <section>
      <ProductForm />
    </section>
  );
};

export default page;
